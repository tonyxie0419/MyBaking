<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:background="@drawable/recipe_list_item_bg"
        android:id="@+id/tv_recipe_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/recipe_item_height"
        android:layout_margin="@dimen/recipe_item_margin"
        android:gravity="center"
        android:textColor="@color/recipe_list_item_text_color"
        android:textSize="@dimen/recipe_item_text_size"
        tools:text="@string/default_recipe_name" />

</FrameLayout>